# Webサイト更新監視システム - セットアップガイド

このプロジェクトは、製品用S3バケットへのデプロイを検知し、自動的に1時間の集中監視を開始します。

## 1. システム構成図 (Architecture)

監視結果を**汎用SNSハブ**に集約することで、Discord以外の通知先も並列で追加できる柔軟な設計です。

```text
[ 監視対象 ]              [ 監視ロジック ]                     [ 通知ハブ (SNS Hub) ]
┌──────────────┐         ┌──────────────────────┐         ┌───────────────────┐
│ 製品用S3バケット │ ──────> │ Schedule Creator     │ ──────> │   SNSトピック     │
│ (Product S3) │ (S3 Ev) │ (Lambda: Node.js)    │ (S3 Upd)│ (WebCheck Alerts) │
└──────────────┘         └──────────┬───────────┘         └─────────┬─────────┘
                                    │ (Create)                      │
                                    ▼                               │ (Fan-out)
                         ┌──────────────────────┐          ┌────────┴─────────┐
                         │ EventBridge Scheduler│ ───────> │  Web Checker     │
                         │ (1hr / 10min rate)   │ (Run)    │ (Lambda: Python) │
                         └──────────────────────┘          └────────┬─────────┘
                                                                    │ (Web Upd)
                                                                    ▼
                                                           ┌──────────────────┐
                                                           │   SNSトピック     │
                                                           │ (WebCheck Alerts) │
                                                           └──────────────────┘
```

## 2. 配信される通知内容

本システムでは、以下の2段階の通知が同じSNSトピック（およびDiscord）に配信されます。

1.  **S3更新通知 (第一報)**:
    *   **タイミング**: S3にファイルがデプロイされた直後。
    *   **フォーマット**: `<URL>のS3が更新されました。 <タイムスタンプ(JST)>`
2.  **Webページ更新通知 (反映確認)**:
    *   **タイミング**: Webサイト（CloudFront等）への反映が確認された瞬間。
    *   **フォーマット**: `<URL>のWebページが更新されました。 <タイムスタンプ(JST)>`

## 3. システムロジック詳解

1.  **デプロイ検知**: S3のイベント通知が `Schedule Creator` を起動。
2.  **通知(1)**: `Schedule Creator` がSNSへS3更新を通知。
3.  **動的スケジューリング**: 1時間限定（10分おき）のスケジューラを生成。
4.  **監視**: スケジューラが10分おきに `Web Checker` を呼び出し。
5.  **通知(2)**: `Web Checker` がサイト比較で差分を検知した場合、SNSへWeb更新を通知。
6.  **並列配信**: SNSに登録されている全通知先（Discord等）へ並列でメッセージが送信。
7.  **自動消滅**: 1時間経過後、スケジュールは自動削除。

---

## 4. セットアップ手順

1.  **設定の記述**: `terraform.tfvars` に、監視対象のURL、製品用バケット名、Discord Webhook URLを記入。
2.  **デプロイ**: `terraform apply` を実行。

---

## 5. この構成のメリット

*   **タイムラグの可視化**: S3の更新からWebへの反映まで、どれくらい時間がかかったかがタイムスタンプで分かります。
*   **SNSハブ設計**: 通知の出口が一本化されており、将来の拡張が容易です。
*   **完全自動化**: 監視の開始から終了（リソース削除）まで、すべて自動で行われます。
